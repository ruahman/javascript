{
  "tasks": {
    "run": "deno run -A ./main.ts",
    "cache": "deno cache --reload deps.ts"
  },
  "lint": {
    "rules": {
      "exclude": [
        "no-inferrable-types",
        "no-unused-vars",
        "prefer-const",
        "no-explicit-any"
      ]
    }
  }
}
