{
  "tasks": {
    "dev": "deno run --allow-all ./hello.ts",
    "shell": "deno run -A .\\Drake.ts shell"
  },
  "lint": {
    "rules": {
      "tags": ["recommended"],
      "exclude": ["no-inferrable-types"]
    }
  }
}
